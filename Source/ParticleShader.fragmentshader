#version 330 core

out vec4 fragmentColor;

in vec2 UV;
in vec4 Color;


void main() {
    //fragmentColor = vec4(texture(texture0, UV).rgb, 0.5f);
    //vec4 texColor = texture(texture0, UV);
   
    fragmentColor = Color ;
    //fragmentColor = vec4(0.8f, 0.5f, 0.8f, 0.6f);

}
